# Web (local Docker)

# Backend API URLs
# - SERVER_URL: preferred at runtime for SSR inside containers (uses Compose service name)
# - VITE_SERVER_URL: used at build time for client/browser code and as SSR fallback
SERVER_URL=http://server:8080
VITE_SERVER_URL=http://localhost:8080
