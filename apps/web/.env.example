# Backend API URL
# Development: Leave empty or use relative URL (proxied via Vite to localhost:3000)
# Production: Use full API subdomain URL (e.g., https://api.aipacto.com or https://aipacto-server.fly.dev)
# Docker local testing: http://host.docker.internal:8080
VITE_SERVER_URL=http://localhost:3000
