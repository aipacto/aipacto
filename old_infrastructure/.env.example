# === REQUIRED ===

# Hetzner Cloud API Token
# Get from: https://console.hetzner.cloud/projects/YOUR_PROJECT/security/tokens
HCLOUD_TOKEN=your-hetzner-cloud-api-token

# === OPTIONAL ===

# Your SSH key name in Hetzner Cloud
SSH_KEY_NAME=your-ssh-key-name

# Domain configuration
PRODUCTION_DOMAIN=aipacto.com
# STAGING_DOMAIN=staging.aipacto.com

# === For GitHub Actions (add as repository secrets) ===

# S3 credentials for Terraform state storage
# Can use Hetzner Object Storage or AWS S3
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=eu-central-1
AWS_EC2_METADATA_DISABLED=true

# Nomad access (after first deployment)
NOMAD_PRODUCTION_ADDR=http://production-server-ip:4646
NOMAD_PRODUCTION_TOKEN=nomad-acl-token-if-enabled

# === PR Environments ===

# Default TTL for PR environments (168h = 1 week)
PR_TTL=168h

# Maximum number of concurrent PR environments
MAX_PR_ENVIRONMENTS=5

# === Container Registry ===

# GitHub Container Registry (default)
REGISTRY=ghcr.io
IMAGE_PREFIX=guillempuche/pacto_chat

# # === Monitoring (optional) ===

# # DataDog API key (if using DataDog)
# DD_API_KEY=your-datadog-api-key

# # Sentry DSN (if using Sentry)
# SENTRY_DSN=https://your-key@sentry.io/project-id

# === Database ===

# PostgreSQL connection for production
DATABASE_URL=postgresql://user:pass@host:5432/aipacto_production

# === Email (for Let's Encrypt certificates) ===

# Admin email for SSL certificates
ADMIN_EMAIL=general@aipacto.com

# === Cloudflare (if using for DNS) ===

# Cloudflare API token for DNS management
CLOUDFLARE_API_TOKEN=your-cloudflare-token
CLOUDFLARE_ZONE_ID=your-zone-id
